{
  "ConnectionStrings": {
    //mysql
    "DefaultConnection": "Server=mysql;Port=3306;Database=db_movie_testing;Uid=developer_agate;Pwd=;Convert Zero Datetime=True;SslMode=none;AllowPublicKeyRetrieval=True",
    "DbPassword": "root", //secret

    //mongo
    "Host": "localhost:27017",
    "HostPrefix": "mongodb://",
    "Username": "", //secret
    "Password": "", //secret

    //mythic account
    
    
  },

  //rabbitmq
  "RabbitMqServer":{
    "HostName": "rabbitmq-server",
    "UserName": "guest", //secret
    "Password": "guest", //secret
    "Port": "5672"    
  },

  //redis server
  "RedisServer": {
      "Enabled": true,
      "CacheConfiguration": "redis-master", //secret if using password parameter
      "ConnectionMultiplexer": "redis-master:6379,allowAdmin=true", //secret if using password parameter
      "InstanceName": "movie:testing:"
  }
}
